OPENWEATHER_API_KEY=
DATA_GOV_IN_API_KEY=
# If 3001 is busy, use 3002 (or any free port). Vite’s dev proxy reads this from server/.env.
PORT=3001
# Must match the ML port (default 5050). If you use PORT_ML=5051 npm run dev:ml, set 5051 here.
ML_SERVICE_URL=http://127.0.0.1:5050
